const ImageSkeleton = () => (
    <div class="w-1/2 flex items-center justify-center ">
        <p className="text-gray-500 animate-pulse text-32">Loading...</p>
    </div>
)

export default ImageSkeleton;